on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:20
    steps:
      - run: node --version
      - run: npm --version
      - run: npm install
      - run: json-server-auth -p 3000 ./backend/db.json
      - run: node main.js
